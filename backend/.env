# n8n Webhook URLs
# สำหรับส่งข้อมูลผู้ใช้ไป n8n เพื่อ AI Scoring และบันทึก Database
# (สำหรับขณะนี้ ใช้ localhost แบบ internal API)
N8N_SCORER_WEBHOOK=http://localhost:8001/webhook/scorer
N8N_SUMMARY_WEBHOOK=http://localhost:8001/webhook/summary

# FastAPI Settings
DEBUG=True
